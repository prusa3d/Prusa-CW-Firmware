os: linux
dist: xenial
language: cpp
script:
  - git fetch --unshallow || if [ $? -eq 128 ]; then true; else false; fi
  - git rev-list --count HEAD
  - bash -x build.sh
deploy:
  provider: releases
  token: $ACCESS_TOKEN
  file:
    - build/Prusa-CW1-Firmware-en.hex
  skip_cleanup: true
  draft: true
  prerelease: true
  on:
    tags: true
